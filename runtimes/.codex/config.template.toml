# Codex configuration template. Drop into .codex/config.toml in your repo.
# This file is tracked. Per-user secrets belong in environment variables.
# Generated from runtimes/mcp/servers.yaml. Do not edit by hand.
# Re-run runtimes/mcp/render/render_codex.py to regenerate.

approval_policy = "on-request"
sandbox_mode = "workspace-write"

# ─── MCP Servers ──────────────────────────────────────────────────────────

[mcp_servers.filesystem]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-filesystem", "."]

# Per-tool approval mode for filesystem operations:
[mcp_servers.filesystem.tools.move_file]
approval_mode = "approve"

[mcp_servers.filesystem.tools.create_directory]
approval_mode = "approve"

[mcp_servers.filesystem.tools.write_file]
approval_mode = "approve"

[mcp_servers.github]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-github"]
# Requires these environment variables, exported in your shell or a gitignored .env: GITHUB_PERSONAL_ACCESS_TOKEN

[mcp_servers.memory]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-memory"]

[mcp_servers.sequential-thinking]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-sequential-thinking"]

[mcp_servers.playwright]
command = "npx"
args = ["-y", "@executeautomation/playwright-mcp-server"]

# Per-tool approval mode for playwright operations:
[mcp_servers.playwright.tools.playwright_navigate]
approval_mode = "approve"
