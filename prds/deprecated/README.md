# Deprecated PRDs

PRDs that have been superseded but are kept for context. Filename should retain the original slug. Each deprecated PRD's frontmatter must:

- Set `Status: Deprecated`.
- Add a `Superseded-By:` field pointing to the replacement PRD path.

Don't delete deprecated PRDs — context decays fast, and "why did we change direction?" is a question that comes up months later.
