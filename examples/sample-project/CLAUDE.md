# CLAUDE.md — User Search Sample Project

@AGENTS.md

Claude Code delegation shim for the worked example. `AGENTS.md` is the
canonical, vendor-neutral source of truth; do not duplicate its contracts here.

This project's consumer runtime lives in `.claude/`. Its executable project
hooks are wired through `.claude/settings.json`; `.claude/hooks/hooks.json` is
the annotated authoring source.
