# Deprecated PRDs

Superseded PRDs kept for context. Each deprecated PRD's frontmatter must:

- Set `Status: Deprecated`.
- Add a `Superseded-By:` field pointing to the replacement PRD.

Don't delete deprecated PRDs - context decays fast, and "why did we change direction?" is a question that comes up months later.
