# Active PRDs

In-flight Product Requirements Documents. PRDs here are at one of: `Draft`, `Under Review`, `Approved`, or `In Implementation`. Once shipped, they move to `../archive/`. Once superseded, to `../deprecated/`.

Filename convention: `<feature-slug>.md` matching the slug used in `specs/<slug>/` and any feature-scoped prompt directories.
